\ProvidesPackage{myFigures}[2022/02/24 package myFigures; Combines useful figure-related packages, no new definitions.]	
%%%%%%
%
% Figures
%
%%%%%%
\usepackage{subcaption}     % Provides the `subcaptionbox` environment (agnostic about its sub being figure or table, 
                            % as opposed to `subfigure` and `subtable`) with (sub)captions for each subfigure, and `\subref`
                            % command to refer to the subfigures.
                            % See the CTAN documentation: http://mirrors.ibiblio.org/CTAN/macros/latex/contrib/caption/subcaption.pdf
\usepackage{graphicx}       % To easily insert images, use the \includegraphics command.
